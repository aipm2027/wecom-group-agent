# 真机与集成测试

本文档描述 `wecom-group-agent` 的手动真机测试步骤、已知验证结果及待补测试项，面向接手项目的工程师。

---

## 1. 手动真机测试步骤

### 1.1 准备 `.env`

```bash
cp .env.example .env
# 编辑 .env，填入 LLM_API_KEY（当前用 StepFun）
```

`.env` 已被 `.gitignore` 忽略，**切勿提交密钥**。

### 1.2 交互式真机测试（Mac 可跑）

```bash
MOCK=1 HANDLER=llm python3 main.py
```

输入格式：

```
chat_id|sender|content
```

示例：

```
群A|张三|@bot 你好
私聊-客户A|李四|那个每日坚果礼盒多少钱
```

> 内容含 `@bot` 视为在群里 @ 了机器人（会被剥掉后再进内容）。

### 1.3 脚本式批量测试

```bash
MOCK=1 HANDLER=llm MOCK_SCRIPT=examples/sample.json python3 main.py
```

脚本文件格式见 `[examples/sample.json](../../examples/sample.json)`，每条是一个 `dict`，可含 `Message` 的任意字段。

### 1.4 断网降级验证

1. 正常启动：`MOCK=1 HANDLER=llm python3 main.py`
2. 发送一条消息，确认有正常回复。
3. 断网（或临时改 `.env` 中 `LLM_BASE_URL` 为一个无效地址）。
4. 再发送一条消息，应看到回复变为：`"不好意思，我这边有点忙，稍后回复你哈~"`。
5. 观察 stderr 有降级日志，但进程不崩溃。

---

## 2. 已知验证结果

| 测试项 | 状态 | 备注 |
|--------|------|------|
| 离线 5 项单测 | **全部通过** | `tests/test_llm_offline.py` 5/5 |
| echo 管道回归 | **正常** | `MOCK=1 HANDLER=echo python3 main.py` 复读机链路可交互/可脚本 |
| StepFun 实测真回复 | **通过** | 维护/询价走知识库/多轮记忆/投诉转人工/防注入 等场景均得到合理回复 |
| 断网降级 | **正确** | 拔掉网络或填错 URL 后，`LLMHandler` 返回兜底话术，stderr 打印降级信息，进程不崩溃 |
| 真实企微 hook | **未接** | `ntwork` 仅 Windows，当前 Mac 无法运行 |
| 微信客服适配器 | **未接** | 合规 1:1，待后续实现 |

---

## 3. 待补测试

> 微信客服适配器离线测试**已补齐**：`tests/test_kf_offline.py` 注入假 HTTP，覆盖 AES（FIPS-197 向量）、PKCS7、加解密 round-trip、篡改检测、URL 验证、回调映射、`send`、`errcode`/降级等，纯离线。

| 待补项 | 说明 | 阻塞条件 |
|--------|------|---------|
| **ntwork 适配器测试** | `adapters/ntwork_wecom.py` 当前为 stub，真实 hook 需在 Windows + 企微客户端上验证字段映射（`conversation_id`→`chat_id`、`local_id`→`msg_id` 等）和收发稳定性。 | 仅 Windows；有封号风险；需实机 |

> `ntwork` 适配器的实现要点与字段映射已写在 `[adapters/ntwork_wecom.py](../../adapters/ntwork_wecom.py)` 顶部注释中，供后续接手参考。

---

*文档最后更新：2026-07-07*

[返回测试总览](00-总览.md)
