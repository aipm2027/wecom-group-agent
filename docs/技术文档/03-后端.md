# 技术文档·后端(核心引擎)

> 本文档定位：面向接手工程师的代码层参考，聚焦 `core/message.py`、`core/router.py`、`core/session.py`、`main.py` 四个核心文件，解释数据模型、路由流程、会话管理与入口装配。运营侧用法请参考 [需求文档](../需求文档/00-总览.md) 与 [测试文档](../测试文档/00-总览.md)。
> 返回总览：[技术总览](00-总览.md)

---

## 1. Message 规范化模型

`core/message.py` 定义了全链路唯一的消息数据结构，屏蔽 mock / ntwork hook 等不同传输层的字段差异。

```python
@dataclass
class Message:
    chat_id: str                      # 群/会话唯一 id
    chat_type: str                    # "group" | "single"
    msg_id: str                       # 用于去重
    sender_id: str                    # 发送者 id
    sender_name: str                  # 发送者昵称
    content: str                      # 文本内容
    msg_type: str = "text"            # 先只处理 text
    is_at_bot: bool = False           # 群里是否 @ 了机器人
    timestamp: int = 0                # 毫秒
    raw: dict[str, Any] = field(default_factory=dict)  # 原始 payload，留存
```

### 字段语义表

| 字段 | 类型 | 说明 |
|------|------|------|
| `chat_id` | `str` | 群/会话唯一标识，由适配器按传输层原字段映射（如企微 `conversation_id`） |
| `chat_type` | `"group" \| "single"` | 区分群聊与单聊，决定路由触发策略 |
| `msg_id` | `str` | 消息唯一 id，用于 `Router` 去重；mock 模式下由适配器自增生成 |
| `sender_id` | `str` | 发送者唯一标识；`BOT_SENDER_ID = "__bot__"` 表示机器人自身 |
| `sender_name` | `str` | 发送者昵称，LLM handler 会在历史记录中拼接为 `"昵称：内容"` 送入 user role |
| `content` | `str` | 文本内容；若为空则在 LLM handler 组装时会被跳过 |
| `msg_type` | `str` | 当前仅支持 `"text"`，非 text 消息触发策略直接拒绝 |
| `is_at_bot` | `bool` | 群聊中是否 @ 了机器人；`MockCliAdapter` 将内容中的 `@bot` 解析为此标记 |
| `timestamp` | `int` | 毫秒时间戳，当前主要留存，未参与限流逻辑 |
| `raw` | `dict` | 原始传输层 payload，供调试或未来扩展字段时使用 |

```python
@property
def is_group(self) -> bool:
    return self.chat_type == "group"
```

---

## 2. Router.on_message 完整流程

`core/router.py` 的 `Router.on_message` 是整条链路入口。流程固定为：

**去重 → 入会话 → should_respond 触发策略 → 限流 → handler 生成回复 → 回发 → 记忆**

```python
def on_message(self, msg: Message) -> None:
    # 1) 去重（重复推送直接丢）
    if msg.msg_id and self._is_duplicate(msg.msg_id):
        return

    # 记入会话上下文（无论是否回复，都保留历史）
    session = self.sessions.get(msg.chat_id)
    session.add(msg)

    # 2) 触发判断
    if not should_respond(msg):
        return

    # 3) 限流
    now = time.monotonic()
    if self._rate_limited(msg.chat_id, now):
        return

    # 4) 生成回复
    reply = self.handler.reply(msg, session)
    if not reply:
        return

    # 5) 回发（先发成功，再更新限流时间戳 & 记入会话上下文）
    self.adapter.send(msg.chat_id, reply)
    self._last_reply_at[msg.chat_id] = now
    session.add(Message(
        chat_id=msg.chat_id,
        chat_type=msg.chat_type,
        msg_id=f"bot-reply-to-{msg.msg_id}",
        sender_id=BOT_SENDER_ID,
        sender_name="Bot",
        content=reply,
        msg_type="text",
    ))
```

### 逐步解释

| 步骤 | 函数/逻辑 | 目的与关键参数 |
|------|-----------|----------------|
| **1. 去重** | `_is_duplicate(msg_id)` | 使用 `OrderedDict` 保存已见 `msg_id`，上限 `DEDUP_MAX = 5000`。命中即丢；新 id 写入，超限时 `popitem(last=False)` 淘汰最旧。 |
| **2. 入会话** | `session.add(msg)` | 无论是否触发回复，**所有消息都记入会话历史**，保证多轮上下文完整。 |
| **3. 触发策略** | `should_respond(msg)` | 独立函数，见第 3 节。非 text / 群聊未 @bot 均在此过滤。 |
| **4. 限流** | `_rate_limited(chat_id, now)` | 每个会话独立限流，`MIN_INTERVAL_SEC = 2.0` 秒。`now - last < 2.0` 时直接静默丢弃。 |
| **5. 生成回复** | `handler.reply(msg, session)` | 调用 `EchoHandler` 或 `LLMHandler`。若返回空或 `None`，流程终止。 |
| **6. 回发** | `adapter.send(chat_id, text)` | 通过当前适配器发出；**先发成功，再更新限流时间戳**，避免发送失败却误记时间。 |
| **7. 记忆** | `session.add(bot_msg)` | 将机器人回复以 `sender_id = BOT_SENDER_ID` 写回 `session`，供后续多轮对话上下文使用。 |

### 关键常量

```python
DEDUP_MAX = 5000          # 去重表最多记住多少个 msg_id
MIN_INTERVAL_SEC = 2.0    # 每个会话两次回复之间的最小间隔
```

---

## 3. 触发策略 should_respond

```python
def should_respond(msg: Message) -> bool:
    if msg.msg_type != "text":
        return False
    if msg.is_group:
        return msg.is_at_bot
    return True
```

### 当前规则

| 场景 | 行为 | 目的 |
|------|------|------|
| `msg_type != "text"` | 不回 | 当前仅处理文本，图片/语音等直接静默 |
| 群聊 (`is_group=True`) | 仅当 `is_at_bot=True` 时回复 | 防刷屏，只在被 @ 时响应 |
| 单聊 (`is_group=False`) | 全回 | 私域 1:1 场景，每条消息都视为客服请求 |

### 如何修改

`should_respond` 是独立顶层函数，与 `Router` 解耦，修改无需动 `on_message` 主流程。

**常见扩展方向：**

- **关键词触发**：在 `msg.is_group` 分支中增加 `or "客服" in msg.content` 等条件。
- **全员回复**：将 `if msg.is_group: return msg.is_at_bot` 改为 `return True`，群聊也全回（适合小群/测试）。
- **白名单/黑名单**：读取 `msg.sender_id` 或 `msg.chat_id` 做过滤。
- **AI 判断**：先调轻量模型判断意图，再决定是否走完整 `handler.reply`。

> 修改后仅影响触发，不影响去重、限流、会话记录等机制。

---

## 4. Session / SessionStore

`core/session.py` 维护按 `chat_id` 隔离的会话上下文。

```python
@dataclass
class Session:
    chat_id: str
    history: Deque[Message] = field(default_factory=lambda: deque(maxlen=MAX_CONTEXT))
    state: dict = field(default_factory=dict)  # 预留：阶段标记 / 变量等

    def add(self, msg: Message) -> None:
        self.history.append(msg)


class SessionStore:
    def __init__(self) -> None:
        self._sessions: dict[str, Session] = {}

    def get(self, chat_id: str) -> Session:
        s = self._sessions.get(chat_id)
        if s is None:
            s = Session(chat_id=chat_id)
            self._sessions[chat_id] = s
        return s
```

### 设计要点

| 项 | 说明 |
|----|------|
| `MAX_CONTEXT = 20` | 每个会话仅保留最近 20 条消息，内存安全。 |
| `history` | `deque(maxlen=20)`，自动淘汰旧消息，无需手动清理。 |
| `state` | 预留字典，未来可存放会话阶段（如"已询价→待下单"）、用户标签等。 |
| `SessionStore.get()` | 懒创建：首次访问 `chat_id` 时自动实例化 `Session`。 |

### 扩展 SQLite 持久化

当前为**内存版**，进程退出后历史丢失。`SessionStore` 被设计为可替换单元：

- 替换思路：保持 `get(chat_id) -> Session` 接口不变，内部改为 `sqlite3` 读写。
- 注意点：若持久化，需把 `deque` 的 maxlen 淘汰逻辑放到查询层（如 `ORDER BY timestamp DESC LIMIT 20`），并在 `add()` 时写入磁盘。
- 机器人记忆（`BOT_SENDER_ID` 消息）也需持久化，否则多轮重启后上下文断裂。

---

## 5. 入口 main.py

`main.py` 负责读取环境变量、装配 Adapter / Handler / Router，然后启动。

```python
def main() -> None:
    load_env_file()
    adapter = build_adapter()
    router = Router(adapter, build_handler(), SessionStore())
    adapter.start(router.on_message)
```

### 5.1 load_env_file

```python
def load_env_file(path: str = ".env") -> None:
    """极简 .env 加载：把 KEY=VALUE 读进 os.environ（不覆盖已存在的）。纯 stdlib。"""
    here = os.path.join(os.path.dirname(os.path.abspath(__file__)), path)
    if not os.path.exists(here):
        return
    with open(here, encoding="utf-8") as f:
        for line in f:
            line = line.strip()
            if not line or line.startswith("#") or "=" not in line:
                continue
            key, _, val = line.partition("=")
            os.environ.setdefault(key.strip(), val.strip().strip('"').strip("'"))
```

- 使用 `os.environ.setdefault`，**已存在的环境变量不会被 `.env` 覆盖**，方便临时调试。
- 纯标准库，不依赖 `python-dotenv`。

### 5.2 build_adapter

```python
def build_adapter():
    if os.environ.get("MOCK") == "1":
        from adapters.mock_cli import MockCliAdapter
        return MockCliAdapter(script_path=os.environ.get("MOCK_SCRIPT"))
    from adapters.ntwork_wecom import NtworkWecomAdapter
    return NtworkWecomAdapter()
```

- 若 `MOCK=1`，强制使用 [MockCliAdapter](../../adapters/mock_cli.py)（Mac 可直接运行）。
- `MOCK_SCRIPT` 传入时启用脚本模式，否则进入交互式 stdin 模式。
- 非 mock 时走 [NtworkWecomAdapter](../../adapters/ntwork_wecom.py)，当前为 Windows 专用 stub。

### 5.3 build_handler

```python
def build_handler():
    handler_type = os.environ.get("HANDLER", "echo")
    if handler_type == "llm":
        from core.llm_handler import LLMHandler
        return LLMHandler()
    from core.handler import EchoHandler
    return EchoHandler()
```

- 默认 `HANDLER=echo`，返回占位复读机。
- `HANDLER=llm` 时加载 [core/llm_handler.py](../../core/llm_handler.py)，走真实 LLM 调用。

---

## 6. 开关与运行命令汇总

### 环境变量速查

| 变量 | 取值 | 默认值 | 说明 |
|------|------|--------|------|
| `MOCK` | `1` / 其他 | 无 | `1` 时强制使用 `MockCliAdapter`（Mac 可运行） |
| `MOCK_SCRIPT` | `examples/sample.json` | `None` | mock 脚本模式路径，缺省为交互模式 |
| `HANDLER` | `echo` / `llm` | `echo` | 回复大脑选择 |
| `ADAPTER` | `mock` / `kf` / `ntwork` | 无（由 MOCK 控制） | 当前 `build_adapter` 实际由 `MOCK` 变量驱动；未来可扩展为按 `ADAPTER` 选择 |
| `LLM_API_KEY` | — | 无 | LLM 调用密钥，从 `.env` 读取（勿提交） |
| `LLM_BASE_URL` | — | `https://api.deepseek.com` | OpenAI 兼容接口地址 |
| `LLM_MODEL` | — | `deepseek-v4-flash` | 模型名；当前 `.env` 使用 StepFun `step-3.7-flash` |
| `LLM_TIMEOUT` | — | `60` | 请求超时秒数 |
| `LLM_MAX_HISTORY` | — | `10` | 送入 LLM 的最近历史条数 |
| `LLM_PERSONA_FILE` | — | `prompts/persona.md` | 人设文件路径 |
| `LLM_KNOWLEDGE_FILE` | — | `prompts/knowledge.md` | 知识库文件路径 |
| `LLM_SYSTEM_PROMPT` | — | 无 | 若设置，直接覆盖 persona + knowledge 的拼装 |

### 常用运行命令

```bash
# 离线 echo 交互（Mac 本地调试）
MOCK=1 HANDLER=echo python3 main.py

# 离线 echo 脚本（自动化验证）
MOCK=1 HANDLER=echo MOCK_SCRIPT=examples/sample.json python3 main.py

# 接 LLM 交互（.env 已配置时 MOCK=1 即可）
MOCK=1 HANDLER=llm python3 main.py
# 或 .env 已设 HANDLER=llm 时：
MOCK=1 python3 main.py

# 离线 LLM 单测（注入假 transport，不耗额度）
python3 tests/test_llm_offline.py

# 9 场景私域维护评测（真调 LLM，限流已设 min_interval_sec=0）
python3 examples/demo_agent.py
```

### 真实企微运行

```bash
# 仅 Windows（需 ntwork 注入 WXWork.exe）
python3 main.py
```

> Mac 当前无法运行真实企微 hook（SIP + 无逆向工具），本阶段以 mock 跑通全链路。
